use std::io  ;
use rand::Rng ;
fn main(){
    nombre = RnG::; // rand::thread_rng().gen_range(1..10)
    println!("Nombre secret (verif {})",nombre)
    println!("Devine mon nombre") ;
    input = String::io ;
    assert_eq!(input,nombre) ;

}